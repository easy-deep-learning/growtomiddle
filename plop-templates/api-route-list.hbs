import { NextRequest, NextResponse } from 'next/server';

import {{pascalCase name}}Model from '@/database/models/{{pascalCase name}}';
import mongooseConnect from '@/database/mongooseConnect';

export const dynamic = 'force-dynamic';

// GET - List all {{lowercase name}}s
export async function GET(request: NextRequest) {
  try {
    await mongooseConnect();
    const { searchParams } = new URL(request.url);
    const pageParam = searchParams.get('page');
    const limitParam = searchParams.get('limit');
    const page = pageParam ? Number(pageParam) : 1;
    const limit = limitParam ? Number(limitParam) : 10;

    const {{lowercase name}}s = await {{pascalCase name}}Model.find()
      .sort({ createdAt: -1 })
      .skip((page - 1) * limit)
      .limit(limit)
      .lean();
    return NextResponse.json({{lowercase name}}s);
  } catch (error) {
    console.error('Error fetching {{lowercase name}}s:', error);
    return NextResponse.json({ error: 'Failed to fetch {{lowercase name}}s' }, { status: 500 });
  }
}

// POST - Create a new {{lowercase name}}
export async function POST(request: NextRequest) {
  try {
    await mongooseConnect();
    const body = await request.json();

    const {{lowercase name}} = new {{pascalCase name}}Model(body);
    await {{lowercase name}}.save();

    return NextResponse.json({{lowercase name}}, { status: 201 });
  } catch (error) {
    console.error('Error creating {{lowercase name}}:', error);
    return NextResponse.json({ error: 'Failed to create {{lowercase name}}' }, { status: 500 });
  }
}

