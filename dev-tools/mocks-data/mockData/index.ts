/**
 * Mock data utilities
 *
 * This module exports helper functions for generating mock data using Faker.js
 *
 * @example
 * ```typescript
 * import { randomFromArray, randomNumber } from '@/utils/mockData';
 *
 * const sizes = ['small', 'medium', 'large'];
 * const randomSize = randomFromArray(sizes);
 * const randomAge = randomNumber(18, 65);
 * ```
 */

export * from '../mockData';
