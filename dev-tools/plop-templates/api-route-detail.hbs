import { NextRequest, NextResponse } from 'next/server';

import {{pascalCase name}}Model from '@/database/models/{{pascalCase name}}';
import mongooseConnect from '@/database/mongooseConnect';

export const dynamic = 'force-dynamic';

// GET - Get a single {{pascalCase name}} by ID
export async function GET(request: NextRequest, context: { params: Promise<{ id: string }> }) {
  try {
    await mongooseConnect();
    const { id } = await context.params;
    const {{pascalCase name}} = await {{pascalCase name}}Model.findById(id);

    if (!{{pascalCase name}}) {
      return NextResponse.json({ error: '{{pascalCase name}} not found' }, { status: 404 });
    }

    return NextResponse.json({{pascalCase name}});
  } catch (error) {
    console.error('Error fetching {{pascalCase name}}:', error);
    return NextResponse.json({ error: 'Failed to fetch {{pascalCase name}}' }, { status: 500 });
  }
}

// PUT - Update a {{pascalCase name}}
export async function PUT(request: NextRequest, context: { params: Promise<{ id: string }> }) {
  try {
    await mongooseConnect();
    const body = await request.json();

    const { id } = await context.params;
    const {{pascalCase name}} = await {{pascalCase name}}Model.findByIdAndUpdate(id, body, {
      new: true,
      runValidators: true,
    });

    if (!{{pascalCase name}}) {
      return NextResponse.json({ error: '{{pascalCase name}} not found' }, { status: 404 });
    }

    return NextResponse.json({{pascalCase name}});
  } catch (error) {
    console.error('Error updating {{pascalCase name}}:', error);
    return NextResponse.json({ error: 'Failed to update {{pascalCase name}}' }, { status: 500 });
  }
}

// DELETE - Delete a {{pascalCase name}}
export async function DELETE(request: NextRequest, context: { params: Promise<{ id: string }> }) {
  try {
    await mongooseConnect();
    const { id } = await context.params;
    const {{pascalCase name}} = await {{pascalCase name}}Model.findByIdAndDelete(id);

    if (!{{pascalCase name}}) {
      return NextResponse.json({ error: '{{pascalCase name}} not found' }, { status: 404 });
    }

    return NextResponse.json({ message: '{{pascalCase name}} deleted successfully' });
  } catch (error) {
    console.error('Error deleting {{pascalCase name}}:', error);
    return NextResponse.json({ error: 'Failed to delete {{pascalCase name}}' }, { status: 500 });
  }
}

