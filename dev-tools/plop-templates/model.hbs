import mongoose, { Document, Schema } from 'mongoose';

import type { {{pascalCase name}} } from '@/database/datatypes/{{pascalCase name}}';

export type {{pascalCase name}}Document = Document & Omit<{{pascalCase name}}, '_id' | 'createdAt' | 'updatedAt'>;

const {{pascalCase name}}Schema = new Schema<{{pascalCase name}}Document>(
  {
    // TODO: Add your schema fields here
    name: {
      type: String,
      required: true,
    },
  },
  {
    timestamps: true,
  }
);

const {{pascalCase name}}Model: mongoose.Model<{{pascalCase name}}Document> =
  mongoose.models.{{pascalCase name}} || mongoose.model<{{pascalCase name}}Document>('{{pascalCase name}}', {{pascalCase name}}Schema);

export default {{pascalCase name}}Model;


