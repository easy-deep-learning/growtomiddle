import mongoose, { Document, Schema } from 'mongoose';

export interface I{{pascalCase name}} {
  _id: string;
  // TODO: Add your fields here
  name: string;
  createdAt: string;
  updatedAt: string;
}

export interface I{{pascalCase name}}Document
  extends Omit<I{{pascalCase name}}, '_id' | 'createdAt' | 'updatedAt'>, Document {
  createdAt: Date;
  updatedAt: Date;
}

const {{pascalCase name}}Schema = new Schema<I{{pascalCase name}}Document>(
  {
    // TODO: Add your schema fields here
    name: {
      type: String,
      required: true,
    },
  },
  {
    timestamps: true,
  }
);

const {{pascalCase name}}Model: mongoose.Model<I{{pascalCase name}}Document> =
  mongoose.models.{{pascalCase name}} || mongoose.model<I{{pascalCase name}}Document>('{{pascalCase name}}', {{pascalCase name}}Schema);

export default {{pascalCase name}}Model;

